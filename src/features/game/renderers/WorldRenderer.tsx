import { baseStyles } from "@/src/styles/styles";
import { View, StyleSheet, Text } from "react-native";

// Minimal world renderer placeholder. Replace with Skia scene when ready.
export function WorldRenderer() {
  return (
  
  <Text style={baseStyles.textTitle}>Fuck you and your mother</Text>

  );
}
